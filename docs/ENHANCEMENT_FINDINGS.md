# Enhancement Findings

This backlog turns the high-level improvement plan into concrete engineering findings. It is also exposed through `GET /api/enhancements`.

## Priority Summary

- P0: 4 findings that block serious production use.
- P1: 4 findings that materially improve trust, operability, and desktop adoption, plus 2 completed operations control findings.
- P2: 6 findings that harden scale, quality, and workflow reach.
- P3: 1 reporting enhancement with fast user-visible value.

## Latest updates

- ENH-020: Connector, workspace, and approval rendering are now retry-safe demand-loaded modules. Each API read and module fetch run concurrently, guarded mutations remain in the trusted controller, view-specific Dutch catalogs load with their views, the initial payload is smaller, operational evidence stays verbatim, and the shared immutable-cache fingerprint covers every initial and deferred command-center asset.
- ENH-006: Setup now joins demo/live mode selection with eight live runtime and provider-write safety checks, prioritized remediation, and a desktop-only redacted support-file action. The file contains no environment values, credentials, logs, connection strings, or user data.
- ENH-011: Trello card and attachment short links are now canonical, exact dependency identifiers. Core critical-blocker detection no longer searches descriptions for card titles, graph edges resolve across full and short Trello identifiers, and board/card reads request only attachment link metadata instead of full preview payloads.
- ENH-028: Workspace owners can preview and opt into bounded retention for terminal operational history, snapshots, performance history, finalized notification receipts, and revoked credentials. Every category is audited before and after deletion, shares a distributed workspace lease, and excludes provider-action, approval, audit, active credential, pending delivery, and current-work evidence.
- ENH-007: Background jobs now use one expiring MongoDB lease per workspace and job across Sneup processes. Active runs heartbeat, only the exact private token can renew or release, crashed instances recover by expiry, webhook events stay concurrent, and Job Health exposes protected/skipped evidence without lease identity.
- ENH-025: Production live startup now fails closed on a MongoDB outage, cleans every partially started resource, and gives Windows users an explicit read-only demo recovery choice. The packaged HAI smoke also found and fixed populated identifiers being emitted as `"[object Object]"`.
- ENH-026: Dashboard and HAI JSON traffic now uses `/api/v1`, one strict envelope, and a server-generated request ID shared by response headers and sanitized logs. Existing API paths and non-JSON provider/export/report protocols remain compatible.
- ENH-027: Workspace managers can pause or percentage-roll out four optional workloads with deterministic subjects, optimistic revisions, bounded cache/history, and live fail-closed behavior. These controls cannot authorize provider writes or weaken approval, audit, emergency-stop, permission, or workspace boundaries.
- AI resilience: Every model-backed chat response now uses a demand-loaded, bounded provider gateway. Offline, auth, rate-limit, timeout, outage, malformed, and oversized-response paths remain useful through deterministic local responses and expose truthful provenance without logging provider bodies.
- ENH-002: Low- and medium-risk decision queue routing is now workspace-configurable, high/critical work remains Robert-owned, and an overdue VA/team item is atomically escalated to Robert with audit evidence. All routing remains internal and cannot create a provider write.
- ENH-018: Scheduled intervention cooldowns are workspace-scoped and configurable per signal from 24 to 168 hours. They only suppress duplicate internal candidates, leave manual requests distinct, and never prepare or perform a provider write.
- ENH-019: Decision queue review windows are bounded from 1 to 168 hours per risk queue. Follow-up and escalation timing is also workspace-scoped, bounded, and auditable; escalation cannot precede follow-up, all scheduled results remain internal approval candidates, and completed actions schedule their follow-up from the same policy.
- ENH-021: A release-time environment check now confirms the persisted-token and connector secrets are present, strong, unique by purpose, and never printed; it refuses demo or non-production environments.
- ENH-023: Sneup now imports only the tokenization, TF-IDF, English AFINN sentiment, and Porter stemming primitives it uses. The Windows package excludes unused Natural language packs, classifiers, WordNet, and their unused storage-client dependencies, reducing the packaged application archive by 39% without changing the NLP contract.
- ENH-024: Workspace owners can permanently delete archived local Sneup data through exact-slug confirmation. The resumable operation covers every workspace collection, removes identity last, retains a minimal receipt, and runs five bounded delayed sweeps for late writes.
- ENH-010: Resend email policies are now available alongside Slack, Teams, and generic webhooks. Every queued or deferred external delivery is atomically claimed before sending; concurrent workers leave the existing claim untouched, and a stranded `sending` claim stays visible for operator evidence rather than being retried blindly.

- ENH-001: TestRail is now the 75th bounded read-only credential-backed connector. It reads selected-project active test-run metadata only, rejects private-network and paginated-overflow targets, and excludes cases, results, descriptions, references, custom fields, attachments, and provider writes.
- ENH-001: BrowserStack is now the 76th bounded read-only credential-backed connector. It reads one capped Automate build page from BrowserStack's fixed API host and excludes public URLs, tags, sessions, logs, browser/device data, and provider writes.
- ENH-001: OneDrive is now the 77th bounded read-only credential-backed connector. It uses a standalone Microsoft OAuth connection with Files.Read only for one capped root-item page and excludes file content, web URLs, permissions, versions, shared links, and provider writes.
- ENH-001: SurveyMonkey is now the 78th bounded read-only credential-backed connector. It reads one capped survey-list page with a View Surveys token and excludes questions, responses, collectors, contacts, links, and provider writes.
- ENH-001: Google Drive is now the 79th bounded read-only credential-backed connector. It uses a standalone metadata-only Google OAuth grant for one capped user-Drive page and excludes file content, web URLs, permissions, owners, shared drives, and provider writes.
- ENH-001: Tableau Cloud is now the 80th bounded read-only credential-backed connector. It accepts a personal access token only for a short-lived session, reads capped project and workbook metadata from one allowlisted Tableau Cloud pod, invalidates that session, and excludes descriptions, views, dashboards, data sources, owners, permissions, URLs, tags, content, and provider content writes.
- ENH-001: SharePoint is now the 81st bounded read-only credential-backed connector. Its explicit scope review highlights delegated `Sites.Read.All`; Sneup lists only followed sites, requires one current selection, reads one capped root metadata page with `Files.Read`, and excludes file contents, web URLs, permissions, pages, lists, people, versions, sharing details, and provider writes.
- ENH-001: Xero is now the 82nd bounded read-only credential-backed connector. It requests only `accounting.invoices.read` plus offline access, requires selection of one currently authorized organisation, reads one capped sales-invoice metadata page, and excludes contacts, invoice numbers, amounts, payment data, descriptions, line items, URLs, and provider writes.
- ENH-001: Google Forms is now the 83rd bounded read-only credential-backed connector. It requests only `drive.metadata.readonly`, reads one capped Google Forms metadata page, and excludes form bodies, questions, responses, owners, URLs, collaborators, sharing details, shared drives, and provider writes.
- ENH-001: Mural is now the 84th bounded read-only credential-backed connector. It requests only `workspaces:read` and `murals:read`, requires one currently authorized workspace selection, reads one capped active-mural metadata page, and excludes mural content, widgets, comments, templates, rooms, people, URLs, sharing details, and provider writes.
- ENH-001: Canva is now the 85th bounded read-only credential-backed connector. Its PKCE verifier is encrypted before it enters the signed OAuth state, it requests only `design:meta:read`, reads one capped design-metadata page, and excludes design content, pages, thumbnails, temporary links, owners, folders, assets, comments, approvals, and provider writes.
- ENH-001: QuickBooks Online is now the 86th bounded read-only credential-backed connector. It validates the opaque authorized-company `realmId` from the signed OAuth callback, requests only the Accounting API scope, reads one capped sales-invoice metadata page, and excludes customers, invoice numbers, amounts, balances, payments, estimates, expenses, projects, line items, descriptions, addresses, links, attachments, taxes, and provider writes.
- ENH-001: Power BI is now the 87th bounded read-only credential-backed connector. It requests only delegated `Report.Read.All`, reads one capped report catalog, and excludes report contents, dashboards, datasets, workspace membership, descriptions, URLs, embeds, owners, subscriptions, users, and provider writes.
- ENH-001: Opsgenie is now the 88th bounded read-only credential-backed connector. It requires an explicit US or EU region, verifies the capped current open-alert count before one GET collection read, and excludes descriptions, aliases, responders, owners, teams, schedules, escalation policies, incidents, integrations, URLs, and provider writes.
- ENH-001: Ravetree is now a bounded read-only credential-backed connector. It uses Ravetree's fixed Open API host and capped offset pagination for work-item metadata only, retaining redacted titles, opaque IDs, completion state, and dates while excluding details, people, accounts, contacts, teams, dependencies, time, tags, custom fields, URLs, and provider writes.

## Findings

| ID | Priority | Area | Finding | Next step |
| --- | --- | --- | --- | --- |
| ENH-001 | P0 | Connectors | Sneup now has 117 catalog entries and 113 bounded read-only credential-backed API clients. Scheduled sync uses a two-workspace pool by default (maximum four when configured), while each provider's complete pacing and retry cycle remains serialized process-wide so cross-workspace scaling cannot create concurrent reads against one provider. OAuth access-token renewal is protected by a separate atomic account lease and secret-free audit events. Adobe Creative Cloud reads only capped Libraries metadata from a fixed Adobe endpoint and excludes elements, assets, files, renditions, collaboration, people, links, storage details, comments, and provider writes. Every connector remains provider-write blocked. | Add the next credential-backed provider client only after its current public read contract and retention boundary are verified. |
| ENH-002 | P0 | Autonomy | Approval review uses action-specific fields instead of free-form JSON; Trello targets, provider routing, execution flags, and action type stay protected, every edit requires fresh approval, and move/reassign targets are verified in the current board/workspace. Decision-queue snoozes use an auditable workspace workflow default (1-168 hours, 24-hour fallback) rather than a hard-coded client deadline, while explicit future dates remain available. Workspace managers can configure per-risk decision owners and escalation windows for internal queues; high and critical items remain Robert-owned, and overdue VA/team items escalate atomically with audit evidence. Snoozing, routing, and escalation never prepare or perform a provider write. | Collect live operator evidence before adjusting per-risk ownership or escalation windows. |
| ENH-003 | P0 | Security | Shared identity operations resolve workspace and role context, audit consequential changes, use one-time hashed invitation tokens, reissue failed email invites only through a new link, and scope job history and pause controls to the requesting workspace. Terminal invitations are redacted in bounded workspace batches after a configurable retention period while lifecycle evidence and aggregate audit counts remain. Workspace migration now produces a read-only aggregate preflight for legacy workspace gaps and future PolicyRule/JobControl uniqueness conflicts; apply and connected startup refuse conflicts before any workspace backfill or index mutation, and a connected conflict cannot silently start demo mode. | Capture production preflight and retention-job evidence from a live workspace. |
| ENH-004 | P1 | Trust | Recommendations include inspectable, validated source evidence; the approval workspace summarizes worker follow-ups, responses, overdue work, escalations, and minimum-evidence intervention outcomes without exposing response text. Explicit client/vendor waits remain distinct external-follow-up findings instead of being double-counted as internal blockers or Robert decisions, while genuine blocker labels and legal, contract, payment, or other decision signals retain stricter routing. Card-specific completed, blocked, and needs-help chat updates become auditable response evidence only when they match one already-executed communication intervention for the same workspace, worker, and card. Due follow-ups also expose a classified operator response form that records evidence only against that exact executed communication, derives its linked workspace/card/member/recommendation on the server, and never sends a provider message. Notification history shows a deduplicated compact source preview and a local read-only drilldown for every validated source link. | Done: collect live operator evidence on source coverage before broadening provider support. |
| ENH-005 | P1 | Forecasting | Sneup now models workspace-scoped weekly capacity, allocation, focus time, planned time off, skills, historical card effort, bounded Harvest, Everhour, Toggl Track, and Clockify tracked-time utilization, mapped Float/Resource Guru scheduling, and mapped Google Workspace/Microsoft 365 organizer metadata to return analysis-only P50/P80 ranges with confidence, assumptions, and delivery risks. Toggl and Clockify require one explicit opaque user-ID mapping per capacity profile; no provider profile is retained. Human-confirmed Float/Resource Guru project IDs map to one exact board, are audit-recorded, reject cross-board duplication, and surface board-scoped scheduling evidence without reducing capacity. Calendar evidence excludes event text, attendees, locations, all-day/cancelled/overlong events, and overlapping-time double counting. | Tune capacity and meeting-load thresholds from reviewed production evidence before considering any calibrated, explicitly approved capacity-reduction model. |
| ENH-006 | P1 | Desktop | Installer first-run setup now includes demo/live selection, live redacted runtime checks, prioritized remediation, and a configuration-only desktop support file; release polish remains. | Configure icon, publisher signing, update feed, and clean-VM acceptance. |
| ENH-007 | P1 | Operations | JobRun/JobControl records, stale/failed/paused dashboard health, allowlisted controls, and expiring per-workspace distributed leases prevent duplicate protected work across Sneup processes. Job Health shows active leases and bounded contention evidence; private lease identity is never selected. | Collect representative hosted multi-instance lease-duration and contention evidence before tuning the five-minute recovery window. |
| ENH-018 | P1 | Autonomy | Scheduled board, follow-up, escalation, and team scans use one workspace-scoped per-signal cooldown policy per scan. Each signal retains the 24-hour baseline or extends it up to 168 hours, preventing duplicate internal candidates without affecting manual requests, approval requirements, or provider-write behavior. | Add filterable cooldown-policy history and retention controls after collecting live operator evidence. |
| ENH-019 | P1 | Operations | Overdue scheduled follow-up plans transition atomically from scheduled to due with workspace-scoped audit evidence. Workspace timing policies set internal no-response follow-up from 24 to 168 hours and escalation from 48 to 168 hours, prevent escalation before follow-up, load once per scheduled pass, and are reused when a completed action schedules its follow-up. All resulting candidates remain approval-gated and provider-write-free. | Add filterable timing-policy history and retention controls after collecting live operator evidence. |
| ENH-020 | P2 | Resource | The command center loads only the active view after the initial security context. Connector, workspace, and approval renderers are demand-loaded with concurrent data/module fetch, reused controllers, retry recovery, view-local catalogs, and one immutable content fingerprint across nine assets. Consequential mutation paths remain outside renderers. Mission control uses exact projected reads and bounded evidence ranking, Trello sync uses a capped board pool, approvals combine 12 reads into one scoped aggregate, and aggregate-only response timing retains no request data or identifiers. | Continue decomposing the controller only where authority boundaries and measurable startup savings remain explicit. |
| ENH-008 | P2 | Dashboard | Dashboard HTML, CSS, and JavaScript are external static assets; Helmet enforces self-only script and style sources without `unsafe-inline`. The revalidatable HTML now injects a content fingerprint into JavaScript, CSS, and icon URLs, which are served with immutable one-year caching. Changed assets receive fresh URLs automatically, without a cache-clear step. | Keep CSP and cache-header regression coverage when dashboard assets change. |
| ENH-009 | P2 | AI quality | `npm run evaluate:recommendations` now evaluates overdue blockers, overload, stakeholder commitments, VA-ready work, and ambiguous requests for evidence, Yes/No framing, policy-aligned risk/ownership, exact provider-write payloads, and forbidden autonomous execution. The Enhancements view displays the current score. | Add human-reviewed, de-identified production recommendation examples to the scenario corpus. |
| ENH-010 | P2 | Notifications | Slack, Teams, generic webhooks, and Resend email policies are encrypted, explicit, severity-filtered, daily-idempotent, delivery-ledgered, and support auditable bounded UTC quiet-hour deferral for warning alerts while critical evidence remains immediate. Warning reconciliation evidence can also be sent as a bounded daily digest with validated source links; source deliveries are only marked digested after the bundle is accepted. Explicit active policies can independently deliver a fixed weekly-status report or a capped read-only daily operations brief at chosen UTC times, each with one delivery key per policy/occurrence and observable scheduler health. A bounded 24-hour catch-up window recovers one missed scheduled run, while the delivery ledger is checked before report or brief generation so ordinary 15-minute worker ticks do not rebuild an already-recorded delivery. Queued and deferred deliveries are atomically claimed before any external request, so concurrent workers do not duplicate notifications; unresolved sending claims remain visible for evidence rather than being retried blindly. | Add bounded delivery-age alerting after collecting representative production evidence. |
| ENH-011 | P2 | Data model | Work signals project into normalized WorkItem, WorkActor, WorkContainer, WorkComment, WorkDependency, and WorkEvent graph records with graph-derived Robert/VA/team decision candidates. Trello links retain canonical short-link aliases, exact linked-card evidence replaces description-title guesses in critical blocker detection, and graph targets resolve across Trello full and short identifiers. Connector batches defer stale-edge processing until each provider completes, then persist bounded stale-edge and failure telemetry in Job Health. Job Health compares only aggregate per-provider failures and pacing with up to four recent completed syncs, and visibly flags new failures after a clean baseline or a bounded pacing spike. It retains no credentials and cannot trigger a provider write or project decision. | Tune provider-specific freshness thresholds and regression thresholds from accumulated live telemetry. |
| ENH-012 | P3 | Reporting | Stakeholder-ready reports export in Markdown and PDF, with an explicit approval-gated weekly-status delivery policy for encrypted Slack, Teams, generic webhook, or Resend email destinations. | Collect production stakeholder feedback before expanding beyond the fixed weekly-status format. |
| ENH-013 | P2 | Connectors | PM connectors are onboarded but many providers still need adapter sync implementations. | Finish the production-safe connector onboarding baseline, validate metadata consistency, and ship first-wave adapters for the added catalog providers. |
| ENH-014 | P2 | Resource | In-memory rate limiting now bounds bucket cardinality before admitting a new route/IP bucket, prunes expired buckets before least-recently-used pressure pruning, and exposes aggregate-only capacity/rejection metrics to audited operators. | Tune `SNEUP_RATE_LIMIT_MAX_BUCKETS` and `SNEUP_RATE_LIMIT_PRUNE_SLACK` after observing representative production pressure. |
| ENH-015 | P1 | Connector safety | Account linking stops at a scope-review gate, exposes requested scopes, narrows four provider permissions to read-only equivalents, blocks provider writes within Sneup, and records non-secret scope consent evidence on the account plus workspace audit ledger. Secret-based accounts now expose a bounded, configurable, read-only credential-rotation deadline without storing or transmitting credential material. | Collect operator evidence before changing rotation intervals or consent-retention policy. |
| ENH-016 | P0 | Autonomy safety | Approved Trello writes use an atomic execution claim, reject forged no-approval write records, retain the claim after post-write bookkeeping faults, and provide an operator-only evidence-backed reconciliation flow that never sends a second provider request. Aging health exposes warning and critical evidence gaps to the operator without retrying provider writes. | Add optional delivered alert policies after notification channels are configured. |
| ENH-020 | P0 | Autonomy safety | Approved provider-write payloads now have bounded risk-aware lifetimes (4 hours critical, 24 hours high, 72 hours medium, 168 hours low by default). The executor verifies the exact expiry against both the approval snapshot and recommendation immediately before the atomic claim. Expired or legacy non-expiring approvals are atomically returned to the internal decision queue with an audit event and never reach Trello. | Collect operator evidence before changing the bounded defaults. |
| ENH-021 | P1 | Outcome verification | A bounded scheduled worker now rechecks completed provider actions only after a configurable evidence-settling window. It reads the existing action attempts, recommendations, outcomes, and synced card/response state; skips confirmed or freshly evaluated outcomes; records only materially changed outcome audits; and never creates another provider request or intervention. | Review live outcome evidence before changing the 24-hour default window. |
| ENH-022 | P1 | Learning safety | Recommendation decisions and outcome status now update one workspace-scoped, compact feedback record per recommendation, with a read-only analytics summary. The signal excludes payloads, card text, credentials, and decision notes, and policy/execution code never consults it, so historical acceptance cannot authorize a provider write. | Review real operator feedback volume before adding recommendation-ranking experiments. |
| ENH-017 | P1 | Autonomy safety | Workspace managers can pause individual Trello action types or raise their risk/owner posture. The executor rechecks the effective policy immediately before its atomic claim, the command center exposes bounded workspace policy history, and time-bound pause reviews become overdue without ever re-enabling a provider write automatically. | Add filtering and retention controls for policy history. |
| ENH-023 | P2 | Desktop resource use | The NLP service now directly imports the tokenizer, sentence tokenizer, TF-IDF, English AFINN sentiment, and Porter stemmer it uses rather than Natural's root export. Electron Builder excludes the unused Natural modules, WordNet dictionary, and storage clients from the shipped archive. The verified app archive fell from 109.6 MB to 65.4 MB (40.3%), and the NSIS installer fell from 119.2 MB to 110.0 MB (7.7%). | Revisit the desktop shell only if installer size remains a material operator constraint after real installation feedback. |
| ENH-024 | P0 | Privacy lifecycle | Owner-confirmed archived-workspace deletion uses the same complete collection registry as export, an irreversible deleting state, minimal resumable receipt, bounded recovery, and delayed late-write cleanup. A guarded real-Mongo verifier seeds and removes all 40 collections. | Capture hosted owner acceptance and provider-side grant revocation evidence before multi-tenant production launch. |
| ENH-027 | P1 | Rollout operations | Four optional workloads now have workspace-scoped persisted controls, deterministic canaries, optimistic revisions, a bounded 30-second/250-workspace cache, 50-entry reviewable history, and live fail-closed behavior. | Capture manager acceptance and staged hosted rollback evidence. |
| ENH-028 | P1 | Privacy lifecycle | Owner-controlled workspace retention is disabled by default and exposes bounded preview, reviewed policy windows, exact-slug manual apply, scheduled distributed leases, and pre/post audit evidence. Only terminal operational history, snapshots, performance history, finalized notification receipts, and revoked credentials are eligible; approval, audit, provider-action, active credential, pending delivery, and current-work evidence is protected. | Capture owner-controlled hosted retention volume before tuning defaults. |
| ENH-029 | P1 | Operator guidance | Searchable, context-sensitive in-app help now covers every command-center view, setup, decision safety, and privacy with direct safe-workflow handoffs, keyboard/focus controls, responsive layout, and no server-side resource cost. | Run an owner-led usability and assistive-technology study before changing topic depth or terminology. |

## Recommended Build Order

1. ENH-003, because auth/workspaces define safe ownership boundaries.
2. ENH-001 and ENH-011 together, because real connector sync requires a normalized work graph.
3. ENH-002 and ENH-004, because autonomy should be reviewable and evidence-backed.
4. ENH-007, because production sync needs operational visibility.
5. ENH-006, ENH-008, ENH-009, ENH-010, ENH-012 as polish and workflow expansion.
## 2026-08-09 Work Signals findings

### Implemented

- Demand-load the complete Work Signals and graph review surface instead of parsing it on every startup.
- Register the view-specific Dutch catalog only with the view and preserve operational/provider text verbatim.
- Keep the renderer capability-poor and delegate only named guarded actions to the authenticated controller.
- Validate evidence links as public-facing presentation links: HTTPS only and no embedded username or password. Previously HTML escaping prevented markup injection but still allowed unsafe schemes to become clickable.
- Reset failed lazy-load state so reopening the view performs a real retry instead of preserving a rejected promise.
- Filter normalized graph rows locally without another provider read or API request.

### Next evidence-led opportunities

- Capture authorized live Work Signals from representative Trello and connector accounts and validate stale-edge/recommendation outcomes end to end without widening provider-write authority.
- Run a clean Windows VM and assistive-technology pass at 100%, 125%, 150%, and 200% scaling before setting accessibility or resource budgets.
- Continue extracting the remaining large report/forecast surfaces only where measurement shows an initial-load benefit and guarded action ownership can remain explicit.
